import { OcultarpasswordPipe } from './ocultarpassword.pipe';

describe('OcultarpasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarpasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
